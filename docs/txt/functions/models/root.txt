# 管理ルートディレクトリ管理モデル

## 概要

管理ルートディレクトリを管理するモデルです。

## 配下

### path

管理ルートパスが入っています。

### blocks (fn)

区画フォルダをすべて取得し、blocksオブジェクトの配列を返します。
fnに関数を渡すと、全区画ディレクトリに対して処理を実施してから、配列を返します。
なお、この関数の実行時には空の区画ディレクトリは削除され、ルート直下にファイルが置かれていた場合はアップストリームディレクトリに移動されます。

